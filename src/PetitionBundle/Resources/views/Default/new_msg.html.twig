{% extends 'base_login.html.twig' %}

{% block body %}
    <div id="contact" class="container">

        <div class="well well-sm">
            <h3><strong>Contactez {{ petition_user }}</strong></h3>
        </div>

        <div class="">

            <div class="col-md-4 col-md-offset-1" style="border: 25px solid #DFECEC;">
                {{ form_start(form) }}
                <label style="color: black "><h4> </h4></label>
                {{ form_widget(form.content) }}
                <br>
                <div class=" col-md-offset-5 " ><input style="top: -20px" type="submit" value="Envoyer" class="btn btn-round btn-info"/></div>
                {{ form_end(form) }}

            </div>
        </div>
    </div>

{% endblock %}
